<UserControl x:Class="MiniCast.Client.Controls.ColorGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniCast.Client.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        x:Name="LayoutRoot">

        <Border
            x:Name="imgBorder"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            BorderBrush="White"
            BorderThickness="2">

            <!--<Image
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                x:Name="spectrum">
            </Image>-->

        </Border>

        <Canvas x:Name="canvasSpectrum" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Canvas.Background>
                <LinearGradientBrush x:Name="canvasBrush" EndPoint="1,0"/>
            </Canvas.Background>
        </Canvas>

    </Grid>
</UserControl>
